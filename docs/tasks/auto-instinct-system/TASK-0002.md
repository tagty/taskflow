# TASK-0002: ディレクトリ・初期ファイル作成

**タスクID**: TASK-0002
**タスクタイプ**: DIRECT
**推定工数**: 1時間
**フェーズ**: Phase 1 - 環境基盤
**信頼性レベル**: 🔵 *architecture.md・REQ-012より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/auto-instinct-system/requirements.md) - REQ-012, EDGE-001
- **設計文書**: [📐 architecture.md](../../design/auto-instinct-system/architecture.md)
- **ファイルフォーマット**: [📝 file-formats.md](../../design/auto-instinct-system/file-formats.md)

## タスク概要

`.claude/instincts/` ディレクトリと `.claude/session-log.jsonl` の初期ファイルを作成する。また、`.gitignore` に session-log.jsonl を追加しないことを確認する（instincts/ はバージョン管理対象）。

## 依存タスク

- **前提タスク**: [TASK-0001: hook設定ファイル更新](TASK-0001.md)
- **後続タスク**: [TASK-0003: instinct-logger.sh実装](TASK-0003.md), [TASK-0004: instinct-on-stop.sh実装](TASK-0004.md)

## 完了条件

- [ ] `.claude/instincts/` ディレクトリが作成されている
- [ ] `.claude/session-log.jsonl` が空ファイルとして作成されている
- [ ] `.claude/instincts/` が git 管理対象である（.gitignore に追加されていない）
- [ ] `.gitignore` に `session-log.jsonl` が含まれているか確認（任意：含めるかは設計に準拠）

---

## 実装詳細

### 1. `.claude/instincts/` ディレクトリ作成 🔵

**信頼性**: 🔵 *REQ-012・EDGE-001 より*

```bash
mkdir -p .claude/instincts
```

**実装ファイル**: `.claude/instincts/`（ディレクトリ）

### 2. `.claude/session-log.jsonl` 初期ファイル作成 🔵

**信頼性**: 🔵 *REQ-002・file-formats.md より*

空の JSONL ファイルを作成する。

```bash
touch .claude/session-log.jsonl
```

**実装ファイル**: `.claude/session-log.jsonl`

### 3. `.gitignore` の確認 🔵

**信頼性**: 🔵 *NFR-011 より*

- `instincts/` はバージョン管理対象のため `.gitignore` に追加しない
- `session-log.jsonl` は一時ファイルのため `.gitignore` への追加を検討（設計はバージョン管理対象外とは明示していないが、一時ファイルとして扱う）

---

## 単体テスト要件

### テストケース1: ディレクトリ存在確認 🔵

**信頼性**: 🔵 *EDGE-001 より*

**Given**: セットアップ完了後
**When**: `ls .claude/instincts/` を実行する
**Then**: ディレクトリが存在する

### テストケース2: session-log.jsonl 存在確認 🔵

**信頼性**: 🔵 *REQ-002 より*

**Given**: セットアップ完了後
**When**: `cat .claude/session-log.jsonl` を実行する
**Then**: ファイルが存在し（空でも可）、読み込める

---

## 実装手順

1. `/tsumiki:direct-setup` - ディレクトリ・ファイル作成
2. `/tsumiki:direct-verify` - ファイル存在確認・git status 確認

---

## 注意事項

- `.claude/instincts/` は `.gitignore` に追加しないこと（NFR-011）
- `.claude/instincts/` ディレクトリが自動作成される設計（EDGE-001）を instinct-on-stop.sh 側でも対応するが、初期状態として手動作成する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質
