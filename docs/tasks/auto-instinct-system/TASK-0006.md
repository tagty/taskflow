# TASK-0006: diary-sync.md拡張

**タスクID**: TASK-0006
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 3 - コマンド実装
**信頼性レベル**: 🔵 *REQ-030〜032・ヒアリング確認済み*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/auto-instinct-system/requirements.md) - REQ-030, REQ-031, REQ-032
- **設計文書**: [📐 architecture.md](../../design/auto-instinct-system/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/auto-instinct-system/dataflow.md)

## タスク概要

`.claude/commands/diary-sync.md` を拡張する。既存の `learnings.md` 読み込みに加えて `.claude/instincts/` も分析対象に追加し、confidence `high` の instinct を CLAUDE.md 統合候補として優先提案する。承認後は instinct ファイルに `promoted: true` フラグを付与する。

## 依存タスク

- **前提タスク**: [TASK-0004: instinct-on-stop.sh実装](TASK-0004.md)
- **後続タスク**: なし

## 完了条件

- [ ] `.claude/commands/diary-sync.md` が更新されている
- [ ] `instincts/*.md` が分析対象に追加されている
- [ ] confidence `high` の instinct が CLAUDE.md 統合候補として優先提案される
- [ ] confidence `medium` は参考情報として表示される
- [ ] confidence `low` はフィルタリングされて表示されない
- [ ] 承認後の instinct ファイルに `promoted: true` が付与される

---

## 実装詳細

### 1. 既存の diary-sync.md の確認 🔵

**信頼性**: 🔵 *既存実装から*

`.claude/commands/diary-sync.md` を読み込み、既存のプロンプト構造を把握する。

**実装ファイル**: `.claude/commands/diary-sync.md`

### 2. instincts/ の読み込み追加 🔵

**信頼性**: 🔵 *REQ-030 より*

diary-sync の手順に以下を追加する:
- `.claude/instincts/*.md` を全件読み込む
- confidence 別にフィルタリングする

### 3. 提案優先度ロジック 🔵

**信頼性**: 🔵 *REQ-031・dataflow.md フロー4より*

```
confidence high  → CLAUDE.md 統合候補として優先提案
confidence medium → 参考情報として表示（統合候補には含めない）
confidence low   → 表示しない（ノイズフィルタ）
```

### 4. promoted フラグの付与 🟡

**信頼性**: 🟡 *REQ-032 から妥当な推測*

ユーザーが CLAUDE.md への統合を承認した instinct ファイルの frontmatter に `promoted: true` を追記する。

```yaml
---
slug: bash-git-pattern
promoted: true  # 追加
---
```

---

## 単体テスト要件

### テストケース1: confidence high が統合候補に含まれる 🔵

**信頼性**: 🔵 *TC-030-01 より*

**Given**: `.claude/instincts/bash-git-pattern.md` が confidence: high で存在する
**When**: `/diary-sync` を実行する
**Then**: bash-git-pattern が CLAUDE.md 統合候補として提案される

### テストケース2: confidence low がフィルタリングされる 🟡

**信頼性**: 🟡 *TC-030-02 より*

**Given**: `.claude/instincts/new-pattern.md` が confidence: low で存在する
**When**: `/diary-sync` を実行する
**Then**: new-pattern は統合候補に含まれない

### テストケース3: 統合後の promoted フラグ付与 🟡

**信頼性**: 🟡 *TC-030-03 より*

**Given**: ユーザーが instinct の CLAUDE.md 統合を承認した
**When**: diary-sync が承認処理を完了する
**Then**: 該当 instinct ファイルに `promoted: true` が追記される

---

## 実装手順

1. `.claude/commands/diary-sync.md` を読み込んで既存の構造を把握する
2. `/tsumiki:direct-setup` - instincts/ 読み込みと confidence フィルタリングを追加
3. `/tsumiki:direct-verify` - テスト用 instinct ファイルを用意して動作確認

---

## 注意事項

- 既存の diary-sync の機能（learnings.md の分析・CLAUDE.md への提案）を壊さないこと
- `.claude/instincts/` が存在しない場合（空でも）エラーにならないこと
- `promoted: true` の付与は YAML frontmatter の編集となるため、フォーマットを崩さないこと

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 1 | 0 | 4 |
| 単体テスト | 1 | 2 | 0 | 3 |

- **総項目数**: 7項目
- 🔵 **青信号**: 4項目 (57%)
- 🟡 **黄信号**: 3項目 (43%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: ✅ 高品質（黄信号は diary-sync の既存実装を読んで確定できる）
