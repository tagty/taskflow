# taskflow テスト仕様書（逆生成）

**分析日時**: 2026-03-25
**対象コードベース**: `src/`

---

## 現在のテスト実装状況

### テストフレームワーク
- **単体テスト**: Vitest v4 + @testing-library/react + @testing-library/user-event
- **統合テスト**: 未実装
- **E2Eテスト**: 未実装

### テストファイル一覧

| ファイル | テスト数 | 内容 |
|---|---|---|
| `src/components/TaskStatusButton.test.tsx` | 4 | ステータスボタン表示・クリック |
| `src/app/projects/[id]/TaskList.test.tsx` | 26 | タスク一覧 UI・インタラクション |
| `src/app/today/page.test.tsx` | 9 | Today ページ 見積時間・優先度 |
| **合計** | **39** | |

### カバレッジ概要（実装根拠ベースで推定）

| 対象 | カバレッジ | 備考 |
|---|---|---|
| `TaskStatusButton.tsx` | ~100% | 全ステータス・クリック検証済み |
| `TaskList.tsx` | ~80% | 主要機能は網羅。ステータスグループ順序・タグなしケース等が未検証 |
| `today/page.tsx` | ~70% | 見積時間・優先度は網羅。**滞留タスク表示が未検証** |
| `queries.ts` | 0% | テストなし |
| Server Actions | 0% | テストなし |
| `history/page.tsx` | 0% | テストなし |
| `projects/page.tsx` | 0% | テストなし |
| `GlobalNav.tsx` | 0% | テストなし |

---

## 既存テストの詳細

### TaskStatusButton（4件）

| # | テスト名 | 検証内容 |
|---|---|---|
| 1 | todo のラベルを表示する | `"未着手"` テキスト |
| 2 | doing のラベルを表示する | `"進行中"` テキスト |
| 3 | done のラベルを表示する | `"完了"` テキスト |
| 4 | クリック時に action を呼び出す | `action` が1回呼ばれる |

### TaskList（26件）

| # | テスト名 | カテゴリ |
|---|---|---|
| 1 | タスクのタイトルを表示する | レンダリング |
| 2 | タグバッジを表示する | レンダリング |
| 3 | 期日を表示する | レンダリング |
| 4 | タグフィルターボタンを表示する | フィルター |
| 5 | タグフィルターで絞り込める | フィルター |
| 6 | 編集ボタンクリックで編集フォームを表示する | 編集 |
| 7 | 編集フォームに削除ボタンを表示する | 編集 |
| 8 | キャンセルで編集フォームを閉じる | 編集 |
| 9 | priority がある場合、P{n} バッジを表示する | 表示 |
| 10 | priority が null の場合、バッジを表示しない | 表示 |
| 11 | estimate_minutes=90 → 1h30m | 見積時間 |
| 12 | estimate_minutes=60 → 1h | 見積時間 |
| 13 | estimate_minutes=30 → 30m | 見積時間 |
| 14 | estimate_minutes=null → 非表示 | 見積時間 |
| 15 | estimate_minutes=0 → 0m | 見積時間 |
| 16 | estimate_minutes=120 → 2h | 見積時間 |
| 17 | priority=1（最小値）を表示する | 境界値 |
| 18 | priority=5（最大値）を表示する | 境界値 |
| 19 | 編集フォームに priority・estimate_minutes の入力欄を表示する | 編集 |
| 20 | description が null → 非表示 | 表示 |
| 21 | description がある → タイトル下部に表示 | 表示 |
| 22 | 編集フォームに description textarea を表示する | 編集 |
| 23 | メモボタンクリックでメモセクション表示 | メモ |
| 24 | notesByTaskId にメモがある場合、展開時に表示 | メモ |
| 25 | メモが0件の場合、件数を表示しない | メモ |
| 26 | メモボタンを再クリックでメモセクションを閉じる | メモ |

### TodayPage（9件）

| # | テスト名 | カテゴリ |
|---|---|---|
| 1 | タスクが0件 → 合計を表示しない | 見積時間 |
| 2 | estimate_minutes がすべて null → 合計を表示しない | 見積時間 |
| 3 | 合計 150分 → `2h30m` | 見積時間 |
| 4 | 合計 480分以下 → 過負荷警告なし | 過負荷 |
| 5 | 合計 481分以上 → 過負荷警告表示 | 過負荷 |
| 6 | null を 0 扱いして合計する | 見積時間 |
| 7 | DB から返された順序でタスクを表示する | ソート |
| 8 | P{n} バッジを表示する | 表示 |
| 9 | 見積時間バッジを表示する | 表示 |

---

## 不足テストの優先順位

### 高優先度（重要なビジネスロジックが未検証）

#### 1. TodayPage — 滞留タスク表示
```
現状: listStalledTasks は毎回 [] でモックされており、滞留タスク表示が未検証。
リスク: 滞留警告の表示ロジックが壊れても検出できない。
```

#### 2. queries.ts — updateTaskStatus の completed_at 処理
```
現状: done 変更時の completed_at 設定・done 以外への変更時の null リセットが未検証。
リスク: 完了履歴の日付が壊れても検出できない。
```

#### 3. TaskList — ステータス別グループ順序
```
現状: doing > todo > done の表示順序が未検証。
リスク: 表示順が変わっても検出できない。
```

### 中優先度（UX に関わるが直接の機能は検証済み）

#### 4. TaskList — タグフィルター「すべて」に戻る
```
現状: タグ選択後に「すべて」ボタンで解除する挙動が未検証。
```

#### 5. TaskList — タグなしタスクの表示
```
現状: tags=[] の場合のフィルター挙動が未検証。
```

#### 6. History ページ — 完了タスクの日付グループ表示
```
現状: テストなし。完了日付ごとのグループ化ロジックが未検証。
```

### 低優先度（実装自体はシンプルで壊れにくい）

#### 7. GlobalNav — アクティブリンクのボールド表示
#### 8. Projects ページ — プロジェクト一覧表示
#### 9. Server Actions — バリデーション（priority 1-5, estimate_minutes >= 0）
